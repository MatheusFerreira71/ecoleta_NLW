export interface ItemTitle {
    title: string
};

export interface Point {
    id: number,
    name: string,
    email: string,
    image: string,
    whatsapp: string,
    latitude: number,
    longitude: number,
    city: string,
    uf: string
};
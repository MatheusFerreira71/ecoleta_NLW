export interface ItemTitle {
    title: string
};

export interface Point {
    id: number,
    name: string,
    email: string,
    image: string,
    image_url: string,
    whatsapp: string,
    latitude: number,
    longitude: number,
    city: string,
    uf: string
};